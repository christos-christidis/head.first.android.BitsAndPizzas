<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <!-- SOS: This stumped me for a LONG time. It's only allowed to add id, width & height to an
        included layout, so scrollFlags wouldn't work, it had to be defined in the main definition! -->
        <include
            android:id="@+id/toolbar"
            layout="@layout/toolbar_main" />

        <android.support.design.widget.TabLayout
            android:id="@+id/tabs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </android.support.design.widget.AppBarLayout>

    <!-- SOS: app:layout_behavior tells any toolbar w scrollFlags that I want it to scroll in response
    to the scrolling of this pager. Note that 3 of the 4 fragments of the pager are non-scrollable,
    only TopFragment's layout defines a NestedScrollView -->
    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />
</android.support.design.widget.CoordinatorLayout>